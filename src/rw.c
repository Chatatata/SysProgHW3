#include "rw.h"

ssize_t kmessaged_read(struct file *filep, char __user *buf, size_t count, loff_t *f_pos)
{
    return 0;
}

ssize_t kmessaged_write(struct file *filep, const char __user *buf, size_t count, loff_t *f_pos)
{
    return 0;
}
